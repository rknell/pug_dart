name: pug_dart
description: Native Dart Pug template renderer with a safe expression engine.
version: 2.0.0
repository: https://github.com/rknell/pug_dart

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  path: ^1.9.1

executables:
  pug-dart: pug_dart

topics:
  - templating
  - html
  - pug
  - server-side
  - templates

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
